# Model weights are fetched from Hugging Face, never committed.
models/
*.gguf
*.safetensors

# Upstream fork is cloned by scripts/02-build.ps1, not vendored.
fork/
build*/

# Logs and scratch
*.log

# Python bytecode
__pycache__/
*.py[cod]

# Session-local artifacts (apply_profile.py, build-profile.bat were one-off
# profiler helpers; remote-results/ contains cross-arch bench data we DO track)
apply_profile.py
build-profile.bat
remote-results/apply_profile.py
